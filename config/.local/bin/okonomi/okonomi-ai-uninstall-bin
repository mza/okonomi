#!/bin/bash
# Uninstall a bin from PATH
# Usage: okonomi-ai-uninstall-bin <bin-name>

if [ -z "$1" ]; then
    echo "Usage: okonomi-ai-uninstall-bin <bin-name>"
    echo ""
    echo "Example: okonomi-ai-uninstall-bin my-tool"
    exit 1
fi

~/.local/bin/ai bin uninstall "$1"
