#!/bin/bash
# Launch bin creation flow in floating terminal
exec setsid uwsm-app -- alacritty --class=Imagine -e bash -c "${HOME}/.local/bin/okonomi/okonomi-ai-create-bin; echo; read -n1 -p 'Press any key to close...'"
